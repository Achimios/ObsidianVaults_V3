
OK, 我整理了一下规则

手动 ignore的只有档案室和 环境包等

具体代码项目，内部init，所以自然会被忽略。手动push到另一个库

但是已经建立快照的，被追踪了吧？ignore或局部init后能成功忽略吗？

如何删除旧快照？本地和github都要删。很多旧历史已经不需要了，验证过了目前进度，没BUG

mklink 建立的镜像链接，会不会被git？是git源文件还是链接？

①隐秘文件，我想本地commit，但是不push，怎么办？
②大文件，比如 blender项目，我想push但是不想commit，怎么办？如果做不到，就commit后立刻删除旧快照，效果一样。目的是节约空间。

