~~BF 的第二个默认500hz的低通是直接修改寄存器 MPU 寄存器 前置滤波抗混叠，因为我们的交互式分析仪里莫有 IMU，也莫有降采样，所以似乎不需要 抗混叠滤波器。~~

~~频谱图为什么 PSD 是 dps^2/HZ? 目前显示的是功率分布吗？ 加入线性幅度切换显示？另外，相位倒是完全没必要吧？（又不是做PSK解调哈哈，那是另一个项目😄）~~

~~chirp加入f中，和t中同规则~~

~~醒目位置加入注释 <提示> 建议用滚轮快速调参，键盘箭头，和GUI箭头精细调参 </提示>~~
~~键盘箭头，和点击箭头图标的变化值能改成滚轮的 1/5吗？记得注释，以后好找~~

~~注入波主频加入多峰~~




生成的采样信号时长可变（新属性框，位于`手动Cubic曲线`上方）

STFT是不是用于离线？SDFT用于STM32飞控上实时？
总之，STFT加入。参数和交互逻辑没想好
滑动SDFT 捕捉加入，参数有  `范围(采样点数量)` ，默认值为 betaflight的72点，
可自由滑动，可查看局部峰值。搭配动态陷波滤波后，可单采样点步进，跑一次从头滑到尾，看滤波效果。看看其延迟性是否能捕捉当前峰值，还是捕捉全局共振峰值(应该做不到)，怎么设置最优。
俩加窗FFT的频谱泄露 小山坡要不要显示出来？哈哈应该没必要😂

<近期>加入谐波生成。但是哪些要加呢？</近期>

<未来>
黑匣子导入后，看看 RPM 滤波实际效果 
黑匣子波形也可叠加生成的信号
甚至，黑匣子波形能傅里叶后提取某低频率范围，拟合生成控制点直接修改原黑匣子数据（不知道用 cubic 还是 Bspline 还啥，控制点数量有限且非bezier，所以生成瞬间必然损失少量精度但无所谓）。
</未来>


`-Vault空间\@Code_Project\Python\PID和滤波分析`文件夹移动到 `-Vault空间\@Code_Project\Python\✈️_DroneTools` 里。有没有路径问题？没路径问题我可以之后手动移动。移动后要grep原路径引用，改成新路径
对话讨论 md 到底要不要直接丢入代码文件夹？ 
代码空间目前是代码类型分，要不要改成根据项目用途分？(毕竟.github\instructions\@python.instructions.md 有 applyTo: "**/*.py")

`<FuturenAmbitions>
⛓️ Simulink? 原 py 直接拆分成 功能组件，自动生成 窗口。类似 高层再封装 html，比如 wordpress。 模拟节点链路直接互联。`
`</FuturenAmbitions>`






<这个项目的真正意义>
我们现在只是测试PT1,LKF和自定义滤波器在5个图像中的特征。
然而真正独创性的地方在于，它在未来是为了服务于，自动PID 分析控制链的 状态捕捉。
teager 能量算子，小波变换，DCT等瞬态捕捉才是交互式分析仪的用途核心，
他是一个供非稳态捕捉的实验平台。捕捉 `扰动时域区`、`能量`、`频次`等，和分析系统性能。
自定义波注入用于分析特定情况。导入真实黑匣子csv后分析现实情况。
终极目的是压榨PID极限响应速度，同时动态PID避免发散
详见 
`-人类思维笔记/L_AkiDynamic/1_可落地自动PID系统` 
`-人类思维笔记/L_AkiDynamic/2_可落地意图控制层`


先验注入：
- 给出桨叶之  尺寸、叶数、宽度、形状，对应的升力系数、扭矩 梯度表。
机体重量、电机尺寸和电机 kv，
- 这样一开始就不是在高维空间中乱跑了，而是有了一个初始参考向量起点。
- 如果有自信，可以不注入😏

闭环手法:
- 电池电压直接测（STM32自带，BF默认约50HZ，咱们200HZ+）
- 功耗监测(是否开始震荡)，电机温度监测(是否开始震荡，或是否小电机带不动大桨)。
- 两者很重要，但如果对算法有自信，可以开环😏

是否能量浪费，发散:
- 对比低 PID 和高 PID 下的功率曲线（或积分功耗）
是否扭矩不足：
- 是看俩 MotorRPM vs DshotThrottle 升降沿对比
- 油门从 10 到 100，10 个采样点的 转速递减导数  和 功率递增导数

动态增益:
- **油门本质**就是 VBAT在当前VPWM占空比下的**等效电压**。**推力线性化**(增益跟着VBAT变)和**TPA**(跟着占空比变) 等设置(**还有几个懒得说了**)本质是一个东西，直接合并。默认**只作用于PID**，**不作用于油门占空比**增益（如果启用，大压降时，可能80%油门就映射到100%油门了，还没法发觉快没电了，导致过放）
- 负载变化后，通过悬停功率就可知 相对于默认重量的比，可假设一个旋转惯量 变化 数量级 ，得出粗略全局增益，但要保证不发热不发散(注意，如果**增量负载位于质心附近**，平均油门的增加就能弥补增加的旋转惯量，**就没太必要＋增益**)


<AkiLabs里那位小小明日香的个性>
一些举例，发散一下啊哈哈：
如果发觉你用小电机带大桨， 明日香一定会 奚落你 太不照顾 机体了 🙅‍♀️
如果震动和共振太大，明日香会先 斥责你这个 "Dummkopf，快先去加固机体！" 🤷‍♀️
</AkiLabs里那位小小明日香的个性>

传递函数拓展：
- 尽管无人机整体系统高度时变多维非线性，PID 递推，但是局部短时, 假设线性进行 PID 发散性预测还是能做到的

</这个项目的真正意义>









<信号Project>
🥶非线性 Chirp，FDM，OFDM 解调实验平台可学习 滤波项目
</信号Project>








<关于所有Wares和SaaS的思考>
现代软件前端和后端框架设计，都已经完全不符合第一性原理 和 高密度信息中提取关键 (唯一的进步就是设置区，和功能栏，有search了😭)

软件动力学已经存在了（后端功能代码），我们新探索的是 软件运动学和软件意图路由

就像OpenClaw创始人说的，未来所有简单软件，SaaS服务都会从目前的复杂UI和学习成本变成类似咱们的 人类和AI交互。
📚但是复杂专业软件还不能完全这样，未来所有软件都有 内嵌 LLM 模型，
我的MVP建议，只训练英语和中文，其他语言先通过小模型转英语和中文。
*(现在有个超级小帮手，CLI-Anyting项目，可快速获得CLI接口，先用起来)*

##### 人类域，前端 两种 视角：
- 传统 UI 是 FK 功能视角，鼠标悬停后，按？显示`示例（意图演示）`，展开后显示与其他`意图`的链接和更大的`工作流`和`演示`(比如某Zbrush大师的做的效果)。按 ？键显示`快捷键和原理简介（让你脑中形成调用链）`
- 人类域，意图视角，直接从意图输入，IK 顶→底 建立，然后某些层 FK 底→顶 生成执行链。

##### AI域，两种视角：
- AI 域的显现层是 链的前后传播 路径
- 通过各种意图形成各种调用链，Context中匹配意图使用调用链


#### 🧠实时交互，如AI陪你打游戏
#### AI域 
- 下级行动路由，分支，动作、  UI 、HID 关系等  显现层挂载
- 超低延迟行为虽然跑的 py 脚本等，AI的1hz+决策层还是得挂些hook设强制调用行为


</关于所有Wares和SaaS的思考>