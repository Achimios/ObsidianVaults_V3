# -📇 注释码表 — 索引码表

> **用途**：登记所有 `>v<` 特殊标注字符串，方便全项目搜索和 AI 快速定位关键点。

## 格式规则

```
>v<  + 意义EMOJI + 主标题 - 副标题 - 额外注释
```

- `>v<` = 标注起始符
- 码表只记录到**主标题**，副标题留在代码注释里
- 搜索：`grep -r ">v<" ./` 即可定位所有标注

---

## 📋 码表（主标题索引）

| 标注字符串 | 含义 | 文件 |
|---|---|---|
| `>v<🧠AHRS算法` | 姿态解算核心，EKF3 实现 | `src/core/ahrs/ahrs.rs` |
| `>v<➰环形缓冲区` | Lock-free SPSC 实现，高吞吐IO | `src/io/ring_buffer.rs` |
| `>v<🌡传感器校正` | IMU 温度补偿 + 零偏校准 | `src/sensors/calibration.rs` |
| `>v<⚡中断处理` | DMA 完成中断，不得阻塞 | `src/hal/dma_handler.rs` |
| `>v<📐四元数运算` | 归一化防止数值漂移 | `src/core/quaternion.rs` |

---

## 🔧 维护 Checklist

- [ ] 定期运行 `grep -r ">v<" ./src` 比对此表
- [ ] 码表有但代码无 → 删除该行
- [ ] 代码有但码表无 → 补录到上表
